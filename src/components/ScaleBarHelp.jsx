import React from 'react';

const ScaleBarHelp = () => {
  return (
    <div className="help-container">
      <div className="help-text">
        <p>
          Drag & resize the scale bar to match a dimension and enter its length
          to scale the footprint.
        </p>
      </div>
    </div>
  );
};

export default ScaleBarHelp;
